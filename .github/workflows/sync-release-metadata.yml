name: Sync release metadata

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Distribution repo note
        run: |
          echo "Binaries are published from the private source repository."
          echo "This public repository hosts install scripts and GitHub Releases only."
